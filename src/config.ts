/**
 * Which backend the app uses. `true`: Firebase Authentication and Cloud
 * Firestore. `false`: the local, on-device AsyncStorage implementations.
 * Read only by src/app/repositories.ts.
 */
export const USE_FIREBASE = true;
